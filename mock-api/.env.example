PORT=3000
ALLOWED_ORIGINS=*

# Datenbank (nur aktiv wenn USE_DB=true)
USE_DB=false
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=raumklima
MYSQL_PASSWORD=raumklima
MYSQL_DATABASE=raumklima

# Auth für den Ingest-Endpunkt
INGEST_API_KEY=change-me-please

# Docker-Build-Optionen (nur für Firmen-Proxy nötig)
NPM_REGISTRY=https://registry.npmjs.org/
CA_CERT_PATH=
